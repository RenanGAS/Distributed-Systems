Para compilação:

- `make compile`

Para execução:

- `make initServer`
- `make initClient`

Exemplo de uso:

- `CONNECT aluno mudar123`
- `GETDIRS`
- `CHDIR Documents`
- `ADDFILE teste.txt "conteudo do teste"`
- `GETFILES`
- `CHDIR ..`
- `EXIT`